{
  "name": "climbplay-odin",
  "version": "1.0.0",
  "description": "Simulateur d'escalade 3D réaliste",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8000",
    "build": "node build.js",
    "android:build": "npm run build && npx cap sync && npx cap open android"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
